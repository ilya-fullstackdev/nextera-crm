# Nextera CRM

Внутренняя CRM для отдела холодных продаж Nextera Studio: поиск и квалификация лидов, воронка сделок, задачи, передача клиентов менеджерам, отчёты и аудит действий сотрудников.

## Содержание

- [Функционал](#функционал)
- [Роли и права](#роли-и-права)
- [Стек](#стек)
- [Быстрый старт](#быстрый-старт)
- [Тестовые учётные записи](#тестовые-учётные-записи)
- [Структура проекта](#структура-проекта)
- [Файлы](#файлы)
- [Известные упрощения](#известные-упрощения--точки-для-расширения)

## Функционал

### Главная

Персональная сводка при входе: новые лиды за сегодня, задачи на сегодня и просроченные, количество лидов в работе, счётчик переданных менеджеру. Ниже — последние лиды, ближайшие задачи и лента последней активности по команде. У руководителя сводка показывает данные по всем сотрудникам, у оператора и менеджера — только по своим.

### Лиды

Таблица лидов с компанией, контактом, должностью, статусом, приоритетом, ответственным, датой следующего контакта, источником и последней активностью. Поиск по компании/контакту/телефону, фильтры по статусу, приоритету, ответственному, источнику, а также быстрые переключатели «Контакт сегодня» и «Без сайта». По правому клику на строке — быстрая смена приоритета и удаление (только у руководителя). На мобильных — карточный вид с быстрым звонком в один тап.

При создании лида система сама проверяет возможные дубли — по названию компании, домену сайта и телефону — и показывает похожие карточки, прежде чем завести новую.

### Карточка лида

- **Быстрые действия** — позвонить, написать в Telegram, добавить задачу или активность, отредактировать, передать менеджеру, оформить отказ.
- **История** — единая лента активности: звонки, сообщения, письма, встречи, заметки, смены статуса, передачи и отказы, с указанием автора и времени.
- **Задачи** — чек-лист задач по лиду с отметкой «выполнено».
- **Файлы** — загрузка и хранение документов (КП, брифы, ТЗ, договоры) с категорией, размером и автором.
- **Квалификация** — статус ЛПР, уровень потребности, проблема, желаемый результат, сроки сделки, статус и комментарий по бюджету, уровень заинтересованности, текущий подрядчик — сохраняется автоматически при изменении поля.
- **Лица, принимающие решение** — привязка существующих контактов компании или добавление новых.

### Воронка

Канбан-доска по стадиям сделки (от «Новый» до «Сделка»/«Отказ») с перетаскиванием карточек между колонками. Карточка показывает компанию, контакт, приоритет, ответственного и дату следующего контакта. Перенос в «Отказ» или «Передан менеджеру» выполняется только через соответствующие модальные окна с обязательным заполнением причины/данных передачи — так в базе не остаётся сделок без объяснения смены статуса.

### Передача менеджеру

Оператор (или руководитель) оформляет передачу лида менеджеру через форму брифинга: статус ЛПР, суть потребности, ситуация клиента, проблема, желаемый результат, сроки, бюджет, что обсуждалось, возражения, следующий шаг. При сохранении лид переходит в статус «Передан менеджеру», меняется ответственный, и менеджеру автоматически ставится задача связаться с клиентом на следующий день.

### Отказы

Оформление отказа требует выбора причины из справочника (нет потребности, есть подрядчик, недавно делали сайт/редизайн, дорого, нет бюджета, не сейчас, выбрали другого, не удалось связаться с ЛПР, другое) и опционального комментария. Лид попадает в раздел «Отказы» с сохранением причины в истории.

### Задачи

Доска задач с вкладками «Просроченные», «Сегодня», «Предстоящие» и «Выполненные». Задача привязывается к лиду или создаётся как самостоятельная, отмечается выполненной одним кликом. Видны тип задачи, срок, связанный лид и исполнитель.

### Компании и контакты

Справочники компаний и контактов с карточками, поиском и переходом к связанным лидам — единая база вместо дублирования данных клиента в каждом лиде.

### Отчёты (только руководитель)

Выбор периода (сегодня/неделя/месяц/произвольный диапазон) и набор метрик: новые лиды, лиды в работе, найдено ЛПР, квалифицировано, передано менеджерам, сделки, отказы. Распределение лидов по стадиям воронки, разбивка по причинам отказов, эффективность источников лидов (конверсия в сделку), нагрузка по сотрудникам и лента последних действий за период.

### Журнал аудита (только руководитель)

Каждое значимое действие — создание/изменение/удаление сотрудника, смена роли, блокировка, сброс пароля, создание и удаление лидов, смена статуса, передача, отказ, изменения компаний и контактов — пишется в журнал с указанием автора, времени и сути изменения на человеческом языке. Журнал фильтруется по типу сущности и имени сотрудника.

### Сотрудники (только руководитель)

Создание учётных записей (логин, пароль, роль, статус), редактирование, блокировка/разблокировка, сброс пароля с одноразовым показом нового значения. Удаление сотрудника, у которого есть активные лиды или незавершённые задачи, требует сначала выбрать коллегу, на которого всё будет переназначено.

### Глобальный поиск

Поле поиска в шапке ищет по лидам: названию и сайту компании, имени, телефону, Telegram и email контакта — с быстрым переходом к нужной карточке.

## Роли и права

| Роль | Доступ |
|---|---|
| Оператор | свои лиды, задачи, воронка, компании/контакты, передача менеджеру, отказы |
| Менеджер | то же плюс переговоры и сделки по своим лидам |
| Руководитель | полный доступ ко всем лидам и сотрудникам, отчёты, журнал аудита |

Проверка роли выполняется дважды: на клиенте (скрытие пунктов меню/кнопок) и
обязательно на сервере — в каждом Server Component через `requireRole()` и в
каждом API route через `requireApiRole()`. Прямой вызов административного API
оператором или менеджером возвращает `403`.

## Стек

- Next.js 16 (App Router, TypeScript)
- Tailwind CSS v4 (design tokens в `src/app/globals.css`)
- PostgreSQL + Prisma 7 (driver adapter `@prisma/adapter-pg`)
- Аутентификация: httpOnly JWT-сессия (`jose`, 30 дней) + `bcryptjs` для хеширования паролей
- Иконки: `lucide-react`

UI-кит (`src/components/ui`) написан с нуля поверх Tailwind, без сторонних библиотек компонентов.

## Быстрый старт

```bash
# 1. Установить зависимости
npm install

# 2. Поднять PostgreSQL (локально уже установлен через Homebrew)
brew services start postgresql@16
createdb nextera_crm   # если база ещё не создана

# 3. Скопировать .env (уже создан для локальной разработки) и применить миграции
npx prisma migrate dev

# 4. Сгенерировать Prisma Client (обычно не нужно — уже сгенерирован)
npx prisma generate

# 5. Засеять демо-данные
npx prisma db seed

# 6. Запустить dev-сервер
npm run dev
```

Приложение будет доступно на http://localhost:3000.

## Тестовые учётные записи

Публичной регистрации нет — все аккаунты создаёт руководитель. После `prisma db seed` доступны:

| Роль | Логин | Пароль |
|---|---|---|
| Руководитель | `director` | `director123` |
| Оператор | `ivan.petrov` | `operator123` |
| Оператор | `maria.sidorova` | `operator123` |
| Менеджер | `alexey.smirnov` | `manager123` |
| Менеджер | `olga.kuznecova` | `manager123` |
| Заблокирован (для проверки блокировки) | `dmitry.sokolov` | `operator123` |

## Структура проекта

```
prisma/schema.prisma        Схема БД (User, Company, Contact, Lead, LeadActivity,
                             Task, LeadFile, LeadHandover, AuditLog, ...)
prisma/seed.ts               Демо-данные

src/app/(app)/               Защищённые страницы (общий layout с sidebar)
src/app/login/                Страница входа
src/app/api/                  Route handlers (REST API, проверка прав на сервере)

src/components/ui/            Переиспользуемые примитивы: Button, Input, Select,
                               Dropdown, Modal, Badge, Tabs, Table, Tooltip,
                               Avatar, Toast, DatePicker, Card, Timeline, KanbanCard
src/components/layout/        Sidebar, Topbar (глобальный поиск), UserMenu
src/components/employees/     Управление сотрудниками (только руководитель)
src/components/leads/         Список лидов, карточка лида, воронка, задачи
src/components/dashboard/     Главная страница
src/components/reports/       Отчёты и журнал аудита

src/lib/auth/                 Сессии, хеширование паролей, RBAC-гварды
src/lib/prisma.ts             Singleton Prisma Client (passwordHash скрыт
                               из всех выборок через global `omit`)
src/lib/audit.ts              Запись в журнал аудита
src/lib/duplicates.ts         Проверка дублей лидов при создании
src/lib/labels.ts             Русские подписи для всех enum'ов
```

## Файлы

Загруженные файлы лидов (КП, брифы, ТЗ, договоры) хранятся на диске в
`public/uploads/leads/<leadId>/` (до 20 МБ на файл) и не коммитятся в git.

## Известные упрощения / точки для расширения

- Телефония и Telegram-интеграция подготовлены на уровне схемы данных
  (поля `phone`, `telegram`, `LeadActivity.type = CALL/MESSAGE`), но без
  реального подключения к внешним провайдерам — это первое, что стоит
  подключить следующим шагом (см. `src/lib/duplicates.ts` и раздел
  активности лида для точки расширения).
- Email-рассылка и хранение переписки не реализованы, только поле `email`
  у контакта и тип активности `EMAIL`.
